# Customizer

## Production

```bash
make prod-build
make prod-up
```

## Development

```bash
cp config/config.toml.dev config/config.toml

make dev-build
make dev-up
make dev-node-install
make dev-node-build
make dev-go-run
```